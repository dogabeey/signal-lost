import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dogabeey.asteroidbelt',
  appName: 'Asteroid Belt',
  webDir: 'dist'
};

export default config;
